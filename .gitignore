node_modules/
dist/
*.tgz
*.bak
.env
.env.*
.env.local
.env*.local

# Playwright MCP screenshot cache
.playwright-mcp/

# Claude Code per-developer files (see Anthropic CLAUDE.md docs §2, §8)
CLAUDE.local.md
.claude/settings.local.json
.claude/.credentials.json
.claude/backups/
RELEASE_TEST_NOTES.md

# Outputs of `aikit` running against this repo (the kit dogfoods itself for
# local testing). The canonical sources live under catalog/ — these are
# regenerated by `aikit sync` and shouldn't appear in commits.
.aikitrc.json
.aikit/
.claude/agents/
.claude/commands/
.claude/hooks/
.claude/skills/
.claude/aikit-rules.json
.claude/plugin/
.claude/rules/example.md
.cursor/
.windsurf/
.codex/
.gemini/
.github/instructions/
.github/agents/
.github/workflows/agents-md-sync.yml
.github/workflows/aikit-rules.yml
.github/workflows/claude.yml
CONVENTIONS.md
.aider.conf.yml
GEMINI.md
CLAUDE.md
AGENTS.override.md
.mcp.json

# Stale dirs left after removing devcontainer/husky/plugin integrations
# (run `rm -rf .devcontainer .husky` to clean up locally if desired)
.devcontainer/
.husky/
